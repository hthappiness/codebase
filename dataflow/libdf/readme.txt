功能介绍：
	1、该库提供的主要是插件的管理以及插件里工厂的管理，一个进程只需要生成一个实体(单例模式)
	2、
对外接口：
	1、对插件开发端，注册工厂、查找工厂；插件需要提供一整套插件管理的接口供库的调用
	2、对进程开发端，添加插件路径、 插件的加载和卸载
	(针对不同的开发端，是否需要提供两个头文件，以减少开发错误的概率)

插件接口：(插件需要提供的接口)
    1、plugin load
	2、plugin unload
	3、plugin name
	4、plugin version
	
使用方法：

扩展性
1、可扩展的功能
2、接口的扩展性

注意：
1、容错性的保证：接口调用错误的提示，错误码的定义.

工程规范
1、工程目录划分
2、模块划分
3、文件规范
4、函数规范

组件使用规范
1、智能指针
2、c++本身，比如几种构造函数的写法
3、异常exception

写组件文档的习惯
文档模板

git规范

cmake规范

开发流程

关于这个框架
1、consumer和producer的框架，将边界定义的比较清晰，这在一定程度上就是降耦的体现，便于工程维护
2、插件的模式，又增加了扩展性
3、当然，它也有自身的局限性，有些事情做起来太过僵硬；开发者沦为“接口程序员”
4、你的思维方式会被一些乱糟糟的东西困扰，所以，要注重你接触的东西，改善他们，
   这不仅仅是外在的建设，也是对自己的塑造，是内在自我的建设